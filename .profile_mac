
# ommitting this to avoid redundancy with .bashrc
# set PATH so it includes user's private bin if it exists
#if [ -d "$HOME/bin" ] ; then
#    PATH="$HOME/bin:$PATH"
#fi

# To line up with linux
alias ll="ls -laF"

# free up ^s
stty -ixon
